import { ContextCreditNotePipe } from './context-credit-note.pipe';

describe('ContextCreditNotePipe', () => {
  it('create an instance', () => {
    const pipe = new ContextCreditNotePipe();
    expect(pipe).toBeTruthy();
  });
});
