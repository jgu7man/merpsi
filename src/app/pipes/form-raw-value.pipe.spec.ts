import { FormRawValuePipe } from './form-raw-value.pipe';

describe('FormRawValuePipe', () => {
  it('create an instance', () => {
    const pipe = new FormRawValuePipe();
    expect(pipe).toBeTruthy();
  });
});
