import { SubcategoryValuePipe } from './subcategory-value.pipe';

describe('SubcategoryValuePipe', () => {
  it('create an instance', () => {
    const pipe = new SubcategoryValuePipe();
    expect(pipe).toBeTruthy();
  });
});
